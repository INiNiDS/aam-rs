[package]
name = "aam-rs-node"
version = "2.8.0"
edition = "2024"
publish = false

[dependencies]
aam-rs = { path = "../../aam-rs", features = ["translator"] }
napi = { version = "3", default-features = false, features = ["napi8"] }
napi-derive = "3"

[features]
reconstructer = ["aam-rs/reconstructer"]

[build-dependencies]
napi-build = "2"

[lib]
name = "aam_rs_node"
crate-type = ["cdylib"]

