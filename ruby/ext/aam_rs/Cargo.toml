[package]
name = "aam_ruby"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
aam-rs = { path = "../../.." }
magnus = "0.9"

[dev-dependencies]
serial_test = "3.2.0"
